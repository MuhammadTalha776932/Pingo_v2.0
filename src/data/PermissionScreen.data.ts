import { NoPermissionScreenProps } from "./NoPermissionScreen.data"

type PermissionScreenProps = NoPermissionScreenProps;

export const PermissionScreenData:PermissionScreenProps  = {
    title:"Allow geolocation from all sources",
    subTitle:"Set up your phone, so your parents can see your location",
    buttonText:"Got it"
}

export default PermissionScreenData